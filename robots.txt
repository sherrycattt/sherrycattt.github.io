# 2026-07-31: the "User-agent: * / Disallow: /assets/cv/" rule was REMOVED together
# with the CV itself, which is no longer published on this site. A Disallow naming a
# path that no longer exists gives a scraper a hint and gives everyone else nothing --
# the note that used to stand here made exactly that point about this file.
#
# If a CV is published here again, put it ALONE in /assets/cv/ and restore the rule,
# so that one Disallow keeps crawlers off it WITHOUT also de-indexing the paper PDFs
# in /assets/pdf/, which should stay findable by Google Scholar.
#
# HONEST LIMIT, unchanged and still worth stating: robots.txt is a request, not a
# control. Well-behaved crawlers (Googlebot, Bingbot, and the AI agents named below,
# all of which do honour it) obey; a scraper written to harvest addresses will not.
# The only real protection is what a published document itself contains.

# AI / LLM training and retrieval crawlers: blocked from the whole site, not just
# the CV. These fetch pages wholesale to build training corpora or to answer a
# query by quoting the source, so a "no" scoped to one directory misses the point.
# Google-Extended is Gemini-training only and does NOT affect Google Search
# ranking or indexing -- Googlebot is a separate agent and stays allowed above,
# so the papers keep indexing normally.
User-agent: GPTBot
Disallow: /

User-agent: OAI-SearchBot
Disallow: /

User-agent: ChatGPT-User
Disallow: /

User-agent: ClaudeBot
Disallow: /

User-agent: Claude-Web
Disallow: /

User-agent: anthropic-ai
Disallow: /

User-agent: CCBot
Disallow: /

User-agent: Google-Extended
Disallow: /

User-agent: PerplexityBot
Disallow: /

User-agent: Applebot-Extended
Disallow: /

User-agent: meta-externalagent
Disallow: /

User-agent: Bytespider
Disallow: /

User-agent: Amazonbot
Disallow: /

User-agent: Diffbot
Disallow: /

User-agent: omgili
Disallow: /

User-agent: cohere-ai
Disallow: /

Sitemap: https://sherrycattt.github.io/sitemap.xml
